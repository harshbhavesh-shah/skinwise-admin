import ComingSoon from "@/components/ComingSoon";

export default function InventoryPage() {
  return (
    <ComingSoon
      title="Inventory"
      description="Stock levels per product, so low-stock items can be flagged before they run out on the storefront."
    />
  );
}
