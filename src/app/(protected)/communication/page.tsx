import ComingSoon from "@/components/ComingSoon";

export default function CommunicationPage() {
  return (
    <ComingSoon
      title="Communication"
      description="A unified inbox for customer messages and email threads, beyond the per-order status emails already sent from Order History."
    />
  );
}
